# Keep GitHub CLI creds project-local (./.gh, gitignored) instead of the
# shared ~/.config/gh. Auto-loaded by direnv on cd into this repo; run
# `direnv allow` once to enable. Requires the direnv shell hook in your rc.
export GH_CONFIG_DIR="$PWD/.gh"
